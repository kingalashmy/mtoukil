// ignore_for_file: file_names

import 'package:flutter/material.dart';

class Realisations extends StatelessWidget {
  const Realisations({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Suivre Realisations '),
      ),
      body: const Center(
        child: Text('List Realisations '),
      ),
    );
  }
}
